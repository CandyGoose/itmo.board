import { test } from '@playwright/test';

test.describe('Layers', () => {
    test('should move an object to the front layer', async ({ page }) => {

    });

    test('should move an object to the back layer', async ({ page }) => {

    });
});
