import { test } from '@playwright/test';

test.describe('Board Actions', () => {
    test('should copy the board link to clipboard', async ({ page }) => {

    });

    test('should rename the board', async ({ page }) => {

    });

    test('should navigate to the main menu', async ({ page }) => {

    });

    test('should delete the board', async ({ page }) => {

    });
});
