import { test } from '@playwright/test';

test.describe('Switch Organization', () => {
    test('should switch to another organization', async ({ page }) => {

    });

    test('should return to the original organization', async ({ page }) => {

    });
});
