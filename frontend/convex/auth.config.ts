const convexAuthConfig = {
    providers: [
        {
            domain: 'https://flexible-terrier-74.clerk.accounts.dev',
            applicationID: 'convex',
        },
    ],
};

export default convexAuthConfig;
